{
  "name": "nexorum-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Real Solidity smart contracts for the NEX token and staking, replacing the previous fake in-memory demo data.",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "test": "hardhat test"
  },
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^17.4.2",
    "hardhat": "^2.29.0",
    "solc": "^0.8.24"
  }
}
